<h1>This are all the Restaurants</h1>

<% @restaurants.each do |restaurant| %>
  <p><%= link_to restaurant.name, restaurant_path(restaurant) %> - <%= restaurant.address %> - <%= restaurant.category %> - <%= link_to "Add Review", new_restaurant_review_path(restaurant), class: "btn btn-success" %></p>
<% end %>
